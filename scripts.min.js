document.addEventListener('DOMContentLoaded', toggleDarkMode);

function toggleDarkMode() {
  const currentHour = new Date().getHours();
  document.body.classList.toggle('dark-mode', currentHour >= 20 || currentHour < 7);
}
